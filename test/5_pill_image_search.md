# 테스트 케이스

| 카테고리 | 서브 카테고리 | 테스트 항목 (기대 결과) |
| --- | --- | --- |
| 밸리데이션 및 예외 처리 |  | 카메라 및 갤러리 접근 권한이 없을 경우 적절한 권한 요청 및 안내가 발생해야 한다. |
| 기능 동작 | 사진 업로드 | 앞면과 뒷면 사진 영역을 각각 탭하여 정상적으로 이미지가 업로드되는지 확인한다. |
| 기능 동작 | 사진 업로드 | 선택된 이미지가 각 영역에 미리보기(Preview)로 표시되어야 한다. |
| 기능 동작 | 사진 업로드 | 미리보기 상단의 `x` 버튼을 눌러 선택된 이미지를 삭제할 수 있어야 한다. |
| 기능 동작 | 검색 실행 | **다중 이미지 선택:** 갤러리에서 여러 장의 이미지를 동시에 선택 시 첫 2장만 앞/뒷면에 순차 등록되는지 확인한다. |
| 기능 동작 | 검색 실행 | **검색 버튼 활성화 조건:** 앞면과 뒷면 이미지가 모두 등록되어야만 하단의 '적용(검색)' 버튼이 표시되거나 활성화된다. (한 면이라도 없을 경우 '알약의 앞면과 뒷면 이미지가 모두 필요합니다' 경고 모달 노출) |
| 기능 동작 | 검색 실행 | **로딩 및 광고 처리:** 검색 실행 시 화면을 덮는 풀 스크린 로딩('이미지를 분석하여 알약을 찾는 중입니다...')이 뜨며, 전면 광고(`Interstitial Ad`)를 호출한다. 전면 광고를 닫으면 검색 결과로 넘어가는지 확인한다. |
| 기능 동작 | 검색 실행 | **오류 처리:** 이미지 분석(API 호출) 실패 시 '이미지 분석에 실패했습니다. 다시 시도해 주세요.' Alert 모달이 뜨며 검색 화면에 머무르는지 확인한다. |
| 기능 동작 | 검색 실행 | (TODO) 검색 중 뒤로가기나 외부 이탈 시 로딩이 멈추거나 검색이 중도 취소(Cancel)되는지 확인한다. |
| 권한 제어 | 카메라 및 갤러리 권한 | 권한을 허용 했을 때: 정상적으로 카메라/갤러리 기능을 사용할 수 있어야 한다. |
| 권한 제어 | 카메라 및 갤러리 권한 | 권한을 허용하지 않았을 때: 권한이 필요하다는 안내(모달 등)가 노출되며 설정 화면으로 이동을 유도하거나 기능이 제한되어야 한다. |
| 권한 제어 | 카메라 및 갤러리 권한 | 권한 허용을 뒤로가기 등 다른 인터렉션으로 무시하고 닫았을 때: 권한 요청이 취소되고 해당 기능이 실행되지 않으며 이전 상태에 머무른다. |
