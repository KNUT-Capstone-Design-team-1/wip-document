```mermaid
flowchart TD
    Home[홈 화면]
    InitSearchScreenLoading[검색 정보 초기화 로딩 화면]
    PillRecognition[식별 검색 화면]
    SearchLoading[검색 로딩 화면]
    PillList[목록 화면]

    Home -->|식별 검색 클릭| InitSearchScreenLoading

    InitSearchScreenLoading -->|테이블 체크 > API 호출 > 테이블 초기화| PillRecognition

    PillRecognition -->|검색 조건 선택 > 검색 버튼 클릭| SearchLoading
    
    SearchLoading -->|쿼리 실행| PillList
```