```mermaid
flowchart TD
    Home[홈 화면]
    InitSearchScreenLoading[검색 정보 초기화 로딩 화면]
    ImageSearch[이미지 검색 화면]
    SearchLoading[검색 로딩 화면]
    PillSearchResultList[목록 화면]

    Home -->|이미지 검색 클릭| InitSearchScreenLoading

    InitSearchScreenLoading -->|테이블 체크 > API 호출 > 테이블 초기화| ImageSearch

    ImageSearch -->|알약 사진 촬영 혹은 첨부 > 검색 버튼 클릭| SearchLoading
    
    
    SearchLoading -->|제미나이 API 요청 > 쿼리 실행| PillSearchResultList
```