# 화면 별 필요한 상태

### 홈

**본 화면에서만 쓰이는 상태 (화면 전용 상태)**

**비즈니스 상태 (서비스 로직 (API / DB)을 호출하는 상태)**

**여러 화면에서 공유되는 상태**



### 알약 식별 검색

**본 화면에서만 쓰이는 상태 (화면 전용 상태)**


### 알약 이미지 검색

**본 화면에서만 쓰이는 상태 (화면 전용 상태)**


### 알약 검색 결과 목록

**본 화면에서만 쓰이는 상태 (화면 전용 상태)**


### 알약 상세 정보

**본 화면에서만 쓰이는 상태 (화면 전용 상태)**


### 알약 보관함

**본 화면에서만 쓰이는 상태 (화면 전용 상태)**


### 공지사항

**본 화면에서만 쓰이는 상태 (화면 전용 상태)**


### 주변약국

**본 화면에서만 쓰이는 상태 (화면 전용 상태)**


### 설정

**본 화면에서만 쓰이는 상태 (화면 전용 상태)**



# 각 스토어 별 가져야할 상태와 액션




# 각 스토어 별 훅 로직




# 각 훅이 호출되는 UI
